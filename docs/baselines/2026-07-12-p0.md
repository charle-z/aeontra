# P0 baseline — 2026-07-12

## Repository state

- Branch: `cubethon-q3`
- Baseline commit: `ae59762bbb60eb67543c7be2440c00e55f78eeac`
- Working tree before P0: clean
- Documented tool count: 59
- Runtime semantic version locations: duplicated in `cmd/mcp-devbox` and
  `internal/mcpserver`
- Tool count: hard-coded in tests and documentation
- Catalog hash: absent
- Safe version endpoint: absent
- `/healthz`: returns semantic version + commit as plain text
- MCP SSE: `Cache-Control: no-cache`
- MCP JSON responses: no explicit cache policy

## Verification baseline

Previously green:

```text
go test ./... -count=1
go vet ./...
go build ./...
```

Coverage measured before P0:

```text
cmd/mcp-devbox          1.4%
cmd/validation-runner  30.9%
internal/audit         84.0%
internal/config       100.0%
internal/grantadmin    59.6%
internal/mcpserver     56.0%
internal/oauth         85.3%
internal/policy        81.5%
internal/tools         73.0%
```

`go test -race ./...` cannot run in the current local runner because CGO is disabled.
The race gate must run in GitHub Actions with CGO enabled; this baseline is an
environment limitation, not evidence that race tests pass.

## Structural debt

- `internal/mcpserver/tools.go` owns every schema, handler registration, alias, and
  annotation classification.
- `internal/tools.Service` aggregates repository, process, GitHub, Coolify, sandbox,
  validation, plan, and privileged concerns.
- `cmd/mcp-devbox/main.go` owns flags, env loading, dependency composition, OAuth,
  transports, grants, and lifecycle.
- `mcp-devbox-console` is a static single-page showcase, not an operator product.
- Documentation currently disagrees on 57/59 tools and verification dates.

## P0 success criteria

- one build-info source;
- deterministic tool catalog manifest and SHA-256 hash;
- no hard-coded tool count in behavior tests;
- safe runtime version/catalog endpoint;
- no-store/no-cache policy on dynamic diagnostic and MCP JSON responses;
- post-deploy smoke comparison of commit/count/hash;
- evidence distinguishing stale client catalog from stale server deployment;
- no env rename and no tool wire-contract break.

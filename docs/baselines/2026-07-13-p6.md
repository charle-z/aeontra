# P6 closure baseline — 2026-07-13

## Repository state

- Closure branch: `p6-step92-closure`
- P6 observed/remediation base: `112ca8ce06ffdeba570e486a548801ee21692a6f`
- Merged and deployed implementation commit: `539e4d96c95aedd492ac36b428d4159054e183f4`
- Fast-forwarded production branch: `main`
- Application: `jqf7qz5ensoqtvl1tb197gcv`
- Production URL: `https://mcp-devbox-charlez.duckdns.org`
- Public MCP tools: 62
- Deterministic catalog hash: `sha256:e3f0b46c65d3ff85f6820cfde88d522d8c7a8db52377e7f4a40bce2dd6330b9c`

The P6 remediation commit series is:

```text
c54090f Step 91: remediate CI security findings
adc9ad5 Step 91: remove vulnerable bootstrap npm
6b69289 Step 91: record green security evidence
539e4d9 Step 91: trigger dependency review after graph enablement
```

`main` advanced from the Step 90 base to `539e4d9` with `git merge --ff-only`; no merge
commit, force push, history rewrite, new application, or OAuth change was made.

## P6 result

P6 converts the P5 evidence into blocking, least-privilege delivery gates:

1. **Verify:** formatting, atomic full-suite coverage, package-specific coverage, vet,
   build, and pinned actionlint.
2. **Race:** the real CGO-enabled `go test -race ./... -count=1` runs in GitHub Actions.
3. **Static/vulnerability analysis:** pinned Staticcheck and Govulncheck fail closed.
4. **Security evidence:** CodeQL, pull-request Dependency Review, local Docker build,
   SPDX SBOM, Grype JSON validation, and an unchanged High/Critical gate.
5. **Fuzzing:** seven synchronized fuzz targets run on a weekly/manual bounded matrix.
6. **Workflow policy:** repository tests reject unsafe triggers, broad permissions,
   mutable refs/tools, missing timeouts, PR secrets, and production DAST.

The observed failures were fixed rather than hidden:

- `GO-2026-5856` was remediated by Go 1.26.5.
- GNU Wget `CVE-2026-58469`, `CVE-2026-58471`, and `CVE-2026-58472` were removed from
  the final image.
- npm `GHSA-52v5-jr5w-gjxr` and `GHSA-c2c7-rcm5-vvqj` were remediated by installing
  `npm@12.0.1` and removing Alpine's vulnerable bootstrap npm tree.
- All 25 Staticcheck findings were corrected.
- The final container scan proved **zero High/Critical** findings without an ignore,
  allowlist, severity reduction, or `continue-on-error`.

Detailed package, layer, reachability, and before/after evidence is preserved in
`docs/security-reports/2026-07-13-p6-ci-container-findings.md`.

## Pull-request evidence

After GitHub Dependency Graph was enabled, commit `539e4d9` produced fully green
pull-request runs:

```text
CI run=29272847130 conclusion=success
  Verify=success
  Race detector=success
  Staticcheck=success
  Govulncheck=success

Security Evidence run=29272847139 conclusion=success
  CodeQL=success
  Dependency review=success
  Container SBOM and vulnerabilities=success
```

The successful Dependency Review is direct evidence that the repository prerequisite
was enabled and the PR dependency delta passed the configured moderate-or-higher gate.

## Post-merge evidence

The fast-forward push of the same commit to `main` produced:

```text
Dependency graph update run=29273109419 conclusion=success
CI push run=29273109759 conclusion=success
  Verify=success
  Race detector=success
  Staticcheck=success
  Govulncheck=success

Security Evidence push run=29273109780 conclusion=success
  CodeQL=success
  Dependency review=skipped (correct for push)
  Container SBOM and vulnerabilities=success
```

Dependency Review is intentionally pull-request-only; its successful PR conclusion is
recorded above, while the push job was correctly skipped.

## Production evidence

After publishing `main`, one reviewed deployment plan was executed for the existing
application. The MCP restarted during its own replacement, so the tool connection was
lost after the Coolify request. The **deployment id was not returned** to the client,
and it could not be recovered through the available safe deployment-read tools. No id
is invented in this baseline.

The deployment itself is independently proven by production identity:

```text
status=ok
commit=539e4d96c95aedd492ac36b428d4159054e183f4
tool_count=62
catalog_hash=sha256:e3f0b46c65d3ff85f6820cfde88d522d8c7a8db52377e7f4a40bce2dd6330b9c
application_status=running:healthy
```

The connector interruption therefore correlates with the expected MCP self-restart:
production returned healthy with the new exact commit and unchanged catalog. There is
no evidence of VPS saturation, Coolify failure, or a persistent server outage.

## Commit and file audit

The Step 91 range changed 30 versioned files. The changes are limited to:

- three GitHub Actions workflows and their workflow-policy tests;
- Go/toolchain and Docker dependency remediation;
- removal of dead declarations and idiomatic error-string corrections;
- Grype gate/report tests;
- specs, baseline/report/runbook, README, capsule, roadmap, AGENTS, and agent memory.

No public tool name, order, schema, annotation, approval path, successful response,
environment-variable name, OAuth contract, root/mount authority, production target,
or catalog hash changed. No secret, binary, cache, SDK, registry credential, Docker
socket, active production scanner, force-push path, or vulnerability suppression was
added.

## Verification

The closure candidate passed locally:

```text
go fmt ./...
go test ./... -count=1
go test ./... -coverprofile=/tmp/mcp-devbox-cover.out -covermode=atomic -count=1
go run ./cmd/coverage-gate --profile /tmp/mcp-devbox-cover.out
go vet ./...
go build ./...
go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.12
go run golang.org/x/vuln/cmd/govulncheck@v1.6.0 ./...
go test ./internal/workflowpolicy ./internal/grypegate ./cmd/grype-gate -count=1
git diff --check
```

GitHub Actions supplied the authoritative Staticcheck, CGO race, CodeQL, Dependency
Review, Docker build, SBOM, and Grype conclusions.

## Closure

P6 is complete. The required workflows pass, the release image has zero unresolved
High/Critical findings, production serves the exact merged commit, and the 62-tool
public contract remains unchanged. P7 structured observability must begin on a fresh
branch/spec and must not record prompts, source content, secrets, private targets, or
credential-bearing metadata.

# v1.2.25 signed release and device update — 2026-08-27

Status: **dated operational evidence**

This file records one observed release and update sequence. It is not a live identity
source and does not replace the platform-specific installation or rollback runbooks.

## Source and release

- Protected `main` resolved to merge commit
  `194eda433669ab449fd0587e25174fb9d374bec1`.
- GitHub Actions run `33121917992` completed successfully for the official
  `edge-release.yml` workflow with release `v1.2.25` and generation `codex-v5`.
- The immutable `v1.2.25` tag and release target the same commit.
- Linux and Windows archives, checksums, signatures and SBOMs were published.
- The Linux third-party notice asset was published with digest
  `sha256:3b14ac604384f3d4cfd95986d22efa4b0e727227d16e89305a5de80c4365f78a`.
- The Windows third-party notice asset was published with digest
  `sha256:fe5499c43d92d29704c1f91b32c7053b1ac186cd6f4f311dfaf73cf55b93cd30`.

Both signed `stable` channel documents named `v1.2.25`, the exact source commit and
catalog `sha256:e1e4b93f3f15dc38895249e269e0e2d84162c1b8177ae85fab406c14c2bdd7c2`.
Their archive hashes matched the immutable Linux and Windows release assets.

## Linux/Parrot update

The paired `parrot-trusted-linux` device had no active Edge operations before update.
Exactly one official `edge_bundle_update(release="stable")` completed as operation
`eo_5beaffd50b8cae6f95115a45ae330956`.

Post-update status reported release `v1.2.25`, the exact source commit, valid manifest,
compatible components, active service, one daemon, held lock, managed coherence, valid
Bubblewrap/rootless/provider/driver state, retained pairing and no update available.

## Native Windows update

The `AeontraEdge` service was updated once through the active signed binary's
`lifecycle update stable` path in a transient elevated PowerShell process. No persistent
UAC helper or ACL exception was created.

Post-update host evidence showed:

- service `AeontraEdge` running with automatic startup;
- service command rooted at the immutable `v1.2.25` release on the configured fixed
  drive;
- exactly one `windows-agent` daemon from `v1.2.25`;
- the known project `hire-me-landing` reachable through `windows-trusted` and reporting
  a dirty checkout, which this acceptance did not modify;
- server-side `update_available=false`.

The Windows bundle/onboarding diagnostic endpoint returned the bounded code
`diagnostic_unavailable_windows`; release identity was therefore established from the
signed update result, SCM command and process classification rather than claimed as a
successful Windows doctor response.

Twenty older `project-process-worker` processes from `v1.2.24` remained after the
daemon replacement. They were not counted as duplicate daemons and were not stopped:
the public surface could not prove their project ownership or whether their durable
work was still required. This is retained as follow-up process-reconciliation debt.

## Limits

- No live rollback was executed on either platform.
- This was maintainer-operated acceptance, not an independent third-party installation.
- Windows diagnostic parity and ownership-aware reconciliation of retained project
  workers remain open acceptance items.

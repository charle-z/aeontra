# v1.2.24 dual-Edge operational acceptance — 2026-08-27

Status: **dated operational evidence**

This file records one observed release state. It is not a live identity source and does
not change the profile-specific security boundaries in `docs/security.md`.

## Source and control plane

- Source and signed release commit:
  `48e8deb6e45c104736291c4fe883771ec063696a`.
- Signed stable release: `v1.2.24`.
- The backend and stable Front Door served protocol `2024-11-05`, 176 tools and catalog
  `sha256:e1e4b93f3f15dc38895249e269e0e2d84162c1b8177ae85fab406c14c2bdd7c2` from the same
  commit before the device checks.

## Native Windows Edge

The real Windows device was observed with:

- `AeontraEdge` running with automatic startup;
- the active service binary selected from the immutable `v1.2.24` release directory;
- exactly one `mcp-edge.exe` process;
- a registered `windows-workcell` project returning `state=ready`;
- no commit, push or deployment performed by the acceptance probe.

The installation uses explicit roots on a fixed local drive. That layout follows
`docs/install-edge-windows.md`; it does not make the Windows workcell equivalent to the
Linux networkless sandbox or rootless toolbox.

## Linux/Parrot Edge

The paired `parrot-trusted-linux` device initially reported healthy `v1.2.1` with an
available signed update and no active Edge operation. Exactly one official
`edge_bundle_update(release="stable")` installed `v1.2.24`.

Post-update status reported:

- manifest valid and components compatible;
- service active with a single process;
- lock held and coherence managed;
- installed and running commit exactly matching the signed release commit;
- Bubblewrap, rootless engine, provider and driver valid;
- paired identity retained and no further update available;
- registered `download-maze` and `pixelgrama` projects returning `state=ready`;
- no queued or running Edge operations after acceptance.

The canonical `mcp-devbox` project checkout independently returned
`project_checkout_unsafe`. Two other registered projects were ready, so this is retained
as project-checkout debt rather than classified as an Edge daemon or bundle failure.

## Limits of this evidence

- No live rollback was executed during this acceptance.
- This was not an independent third-party clean installation.
- Source, GitHub release, backend deployment and each installed device remain separate
  identities even though they matched in this observation.

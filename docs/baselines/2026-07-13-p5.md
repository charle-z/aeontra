# P5 closure baseline — 2026-07-13

## Repository state

- Branch: `p5-deeper-testing`
- Refreshed base: `origin/main`
- Base/deployed commit: `4a96307925751cf7fbe7a4f8eb801f86c8edc3ad`
- Last implementation commit before this closure record: `5036d524ef69cf9bedf480a40afbf78dd7036b08`
- Public MCP tools: 62
- Deterministic catalog hash: `sha256:e3f0b46c65d3ff85f6820cfde88d522d8c7a8db52377e7f4a40bce2dd6330b9c`
- Production remained on the deployed P4 base throughout the P5 audit.

## P5 result

P5 adds deeper, repeatable evidence without changing runtime authority or the public
MCP contract:

1. **Race detector baseline:** the canonical command is documented. The production
   builder uses `CGO_ENABLED=0`, so the race suite was blocked before execution and is
   explicitly not reported as green. P6 must run the real `CGO_ENABLED=1 go test -race
   ./... -count=1` gate in an ephemeral CI environment.
2. **deterministic concurrency:** bounded exactly-once and concurrent-write tests
   cover access grants, action plans, audit JSONL, and OAuth token state.
3. **Fuzz targets:** curated, secret-free seeds cover jail containment, command policy,
   redaction, grant TTLs, JSON-RPC messages/batches, and action-plan binding/single-use.
4. **Coverage gate:** a tested coverprofile parser and CLI enforce versioned,
   package-specific thresholds for policy, protocol, auth, audit, tools, app, and local
   grant administration.
5. **Hermetic integration matrix:** loopback/temp-state contracts cover stdio/HTTP
   parity, fail-closed bearer/OAuth behavior, runtime identity, local grant approval,
   and planned note approval/execution/replay denial.

No public tool name, order, schema, alias, annotation, handler, approval path,
environment-variable name, command, flag, transport auth contract, tool count, or
catalog hash changed.

## Audit evidence

The Step 78–83 commit series was reviewed against refreshed `origin/main`. Commit
messages contain no `Co-Authored-By` lines or AI signatures. Changed files are Go
tests, the coverage gate command/parser, documentation, specs, and agent memory. No
binary, SDK, cache, secret file, token, credential-bearing configuration, Docker
socket, force-push path, or production scanner was added.

## Verification

The phase passed:

```text
go fmt ./...
go test ./... -count=1
go test ./... -coverprofile=/tmp/mcp-devbox-cover.out -covermode=atomic -count=1
go run ./cmd/coverage-gate --profile /tmp/mcp-devbox-cover.out
go vet ./...
go build ./...
git diff --check origin/main...HEAD
mcp-catalog-smoke against production
```

The package coverage gate passed at closure for all eight security-critical packages.
The still-deployed production baseline reported:

```text
commit=4a96307925751cf7fbe7a4f8eb801f86c8edc3ad
tool_count=62
catalog_hash=sha256:e3f0b46c65d3ff85f6820cfde88d522d8c7a8db52377e7f4a40bce2dd6330b9c
```

## Release posture

P5 is merge-ready. Its release changes test evidence and adds one local/CI coverage
command, but does not add runtime authority or alter the deployed MCP catalog. P6 is
responsible for enabling the real race detector, timed fuzzing, and the proven
coverage/integration gates in CI.

No publish, merge, or deploy was performed while creating this P5 closure baseline.

# Handoff — P12 Parrot onboarding hardening and Cubethon readiness

Branch: `p12-parrot-onboarding-hardening`
Base: `origin/main` at `3946fd7033f28906deb932298387034e2fa27fe8`
Current implementation HEAD: `1e164696bd178de79ff7cab5519426d977e042ae`

Real Parrot evidence:
- runtime `mr_829f6601fca6f887bc2d0133a4c5dff1`;
- workspace `ws_7c4686f5d9244bbad30ae705d4b660c5`;
- state `completed`;
- six sequences;
- exact `edge-smoke.txt` content;
- `git diff --check` exit 0.

Commits:
- `52e88f8` — Fix Parrot Edge runtime hardening.
  - stable `bubblewrap_netlink_route_denied` classification;
  - bounded Bubblewrap verification diagnostics without raw stderr leakage;
  - `AF_NETLINK` in the packaged Edge systemd restriction;
  - early remote failure propagation;
  - transactional local-journal migration and repeatable terminal objectives;
  - WSL `/mnt/wsl/resolv.conf` acceptance without allowing `/mnt/c` or `/mnt/d`.
- `3b05d26` — Add reproducible Parrot onboarding.
  - executable `packaging/parrot/onboarding-preflight.sh`;
  - `packaging/systemd/mcp-devbox-opencode-edge@.service`;
  - canonical Parrot guide and real-host production baseline;
  - roadmap/documentation synchronization and closure tests.
- `1e16469` — Prepare Cubethon submission package.
  - Hosted on CubePath and production badges;
  - corrected public P11.2/P12/Edge status;
  - `docs/cubethon-2026-q3-submission.md` with issue-form content and blockers.

Validation on the clean committed tree:
- all Go packages passed in two serial chunks;
- focused Edge/CLI/docs/onboarding tests passed repeatedly;
- `go vet ./...` passed;
- `go build ./...` passed;
- `git diff --check` passed;
- the monolithic serial test was terminated by the tool environment after passing through `internal/resultstore`; the remaining packages were then run separately and all passed.

Remaining actions:
1. publish the branch and open a PR;
2. require exact-head CI, including Race, Staticcheck, Govulncheck, CodeQL, Bubblewrap, rootless Podman, distributed OpenCode, docs and catalog gates;
3. do not merge/deploy while any check is pending or failed;
4. after deployment, replace the temporary Parrot debug/override service with the packaged reviewed unit and re-smoke;
5. for Cubethon, confirm judge-accessible demo authentication, capture screenshots/GIF, insert the exact Discord username, and submit before 2026-07-26.
